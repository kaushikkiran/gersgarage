header.header
  nav.nav.nav--tours
    a.nav__el(href='/') Ger's Garage
  nav.nav.nav--user
    a.nav__el.nav__el--cta(href='/logout') Logout
    a.nav__el(href='/me')
          //span= user.userName.split(' ')[0]